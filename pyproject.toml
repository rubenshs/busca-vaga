[project]
name = "busca-vaga"
version = "0.1.0"
description = "Exemplo de agentes para busca de vagas disponíveis"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
busca-vaga = "src.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]
